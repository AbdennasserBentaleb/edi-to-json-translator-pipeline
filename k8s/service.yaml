apiVersion: v1
kind: Service
metadata:
  name: edi-translator-service
  labels:
    app: edi-translator-pipeline
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: edi-translator-pipeline
